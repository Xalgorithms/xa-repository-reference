sudo: required
language: ruby
rvm:
  - 2.3.1
  
services:
  - docker

addons:
  apt:
    packages:
      - cmake
  
before_install:
  - "./start_containers.sh"

script:
  - bundle exec rake spec

after_success:
  - "./build_images.sh"
  - "./publish_images.sh"
